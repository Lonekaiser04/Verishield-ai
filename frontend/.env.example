# The frontend uses Vite's dev proxy (see vite.config.js) to forward
# /api requests to the backend at http://localhost:8000 during
# development, so no environment variable is required for local dev.
#
# For a production build served separately from the backend, set:
VITE_API_BASE_URL=http://localhost:8000/api
